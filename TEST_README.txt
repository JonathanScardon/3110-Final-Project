When you want to run tests in general,
make sure you put in terminal
$ export ENV=test

When you're done, make sure you then do
$ unset ENV

This is essential for making sure it exports to the right csvs. 